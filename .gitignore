# Audio binaries are distributed through GitHub Releases, not Git history.
*.mp3
*.m4a
*.wav
*.flac
*.aac
*.ogg
*.opus

# Local working files.
.DS_Store
Thumbs.db
desktop.ini
__pycache__/
*.py[cod]
*.tmp
*.temp

# Generated release logs can be kept locally unless copied into release-records/.
release-tools/logs/
bak/
# Local REC generation manifests may contain absolute source paths.
**/rec-generation-manifest.json
